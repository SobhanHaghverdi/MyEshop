@inject IGroupRepository _groupRepository;
<li class="nav-item dropdown">
    <a class="nav-link dropdown-toggle text-white" href="http://example.com" id="dropdown09" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">دسته بندی ها</a>
    <div class="dropdown-menu" aria-labelledby="dropdown09">
        @foreach (var item in _groupRepository.GetAllCategories())
        {
            <ul class="categories-ul-style">
                <li class="dropdown-li-style py-2">
                    <a href="/Categories/@item.Id/@item.Name">@item.Name</a>
                </li>
            </ul>
        }
    </div>
</li>